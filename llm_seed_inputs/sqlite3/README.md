# sqlite3 — LLM-pregenerated initial inputs (placeholder)

Drop LLM-generated **SQL** inputs here, one valid SQLite SQL script per file with
extension `.sql`. They seed `elfuzz direct sqlite3`'s initial population (copied
verbatim). Filenames are provenance only. `README.md` / dotfiles are ignored.

Until at least one `.sql` file is added, `elfuzz direct sqlite3` errors out.
See [../README.md](../README.md).
