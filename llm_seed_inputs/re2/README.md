# re2 — LLM-pregenerated initial inputs (placeholder)

Drop LLM-generated **regex** inputs here, one valid POSIX regular expression per
file with extension `.re`. They seed `elfuzz direct re2`'s initial population
(copied verbatim). Filenames are provenance only. `README.md` / dotfiles are
ignored.

Until at least one `.re` file is added, `elfuzz direct re2` errors out.
See [../README.md](../README.md).
