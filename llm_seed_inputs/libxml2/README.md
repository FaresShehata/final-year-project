# libxml2 — LLM-pregenerated initial inputs (placeholder)

Drop LLM-generated **XML** inputs here, one valid XML document per file with
extension `.xml`. They seed `elfuzz direct libxml2`'s initial population (copied
verbatim). Filenames are provenance only. `README.md` / dotfiles are ignored.

Until at least one `.xml` file is added, `elfuzz direct libxml2` errors out.
See [../README.md](../README.md).
